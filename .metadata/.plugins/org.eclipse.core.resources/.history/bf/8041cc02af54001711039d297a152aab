/* 역할:
 * => 페이지 컨트롤러나 DAO, Service와 같이
 *    자동으로 객체를 생성해야 하는 클래스에 붙이는 애노테이션이다.
 * => 즉 이 애노테이션이 붙은 클래스는 웹 애플리케이션이 시작할 때 자동으로 객체를 생성한다.
 */

package bitcamp.java93.annotation;

public @interface Component {

}
